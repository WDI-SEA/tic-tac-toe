// Get the cells

// Add event listeners to each of them

// Add click event handler that
//// A. marks the cell
//// B. checks the score
//// C. Switch players


// Decision Algorithm 
// (New user's turn) =>  "Circle"
// (shapeDecider = New user's turn ? o : x) => "shapeDecider = o"
// add classes

// Winning Algorithm
